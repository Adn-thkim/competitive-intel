# ── 의존성 ────────────────────────────────────────────
node_modules/
__pycache__/
*.py[cod]
.venv/
venv/

# ── 환경변수 (API 키 포함) ───────────────────────────
.env
.env.local
.env.*.local

# ── 런타임 캐시 / 생성 데이터 ──────────────────────────
# JSON 파일 캐시 (product_name_normalization.json 등)
data/cache/
# 분석 실행 결과 (로컬 생성 데이터)
data/projects/
# 수집 노드 관측성 산출물 (raw 응답·feature_pool·backlog)
data/collection/
# 사용자 정정 오버라이드 (human_review 편집 — 로컬 사용자 데이터)
data/overrides/

# ── SQLite (향후 마이그레이션 후 생성될 파일) ────────────
*.db
*.db-shm
*.db-wal

# ── macOS 시스템 파일 ───────────────────────────────
.DS_Store
docs/.DS_Store

# ── 로그 ────────────────────────────────────────────
*.log
logs/

# ── 에디터 ──────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── 로컬 개발 가이드 (Claude/Cowork 전용) ──────────────
CLAUDE.md

# ── 로컬 작업 메모 / 리팩터링 백업 ──────────────────────
# 리팩터링 전 원본의 임시 백업 디렉터리(git history로 복원 가능하므로 추적 제외)
backup/
# 개인 작업 예정 메모(GitHub Issues/Projects에서 관리하는 것이 표준)
docs/To Do List.md
data/measurement/
data/debug/
data/reports/
scripts/out/
git/
* 2.*
